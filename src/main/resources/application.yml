spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spboard?serverTimezone=Asia/Seoul&useSSL=false&characterEncoding=utf8
    username: root          # MySQL ??? ? ?? ??
    password: 1234

  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    database: mysql
    hibernate:
      ddl-auto: update

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 15MB

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher   # ★ 레거시 방식 적용

# ?? ?? ?? ?? ? ?? ??
server:
  port: 8084
  servlet:
    encoding:
      force-response: true

# S3 ??
cloud:
  aws:
    s3:
      bucket:
    credentials:
      access-key: AKIA3GYASZGN24SOT2KH
      secret-key: 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
    region:
      static: ap-northeast-2
      auto: false
    stack:
      auto: false